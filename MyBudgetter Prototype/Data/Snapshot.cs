namespace MyBudgetter_Prototype.Data
{
    // Snapshots are JSON Files that hold only the DataEntry data
    // Used for moving data
    // Should be used to populate the database or database should be converted into a snapshot
    public class Snapshot
    {
    }
}
